# =============================================================================
# PRECISION PDF - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local for local development
# Copy this file to .env.production for production deployment
#
# SECURITY NOTICE: This project is currently configured for local development
# with authentication DISABLED. See docs/security/re-enabling-auth.md
# =============================================================================

# -----------------------------------------------------------------------------
# CORE APPLICATION SETTINGS (Required)
# -----------------------------------------------------------------------------

# Your Convex deployment URL - automatically set by Convex CLI during dev
# For production, get this from your Convex dashboard
NEXT_PUBLIC_CONVEX_URL="https://your-deployment-name.convex.cloud"

# Your application's public URL
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Alternative site URL format (used by some services)
SITE_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# CLERK AUTHENTICATION (Currently Disabled for Local Development)
# -----------------------------------------------------------------------------

# Clerk publishable key (safe to expose to frontend)
# Development: pk_test_xxx
# Production: pk_live_xxx
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_your-clerk-publishable-key-here"

# Clerk secret key (server-side only, never expose)
# Development: sk_test_xxx
# Production: sk_live_xxx
CLERK_SECRET_KEY="sk_test_your-clerk-secret-key-here"

# Clerk webhook secret for syncing user data
# Generate with: openssl rand -base64 32
CLERK_WEBHOOK_SECRET="whsec_your-clerk-webhook-secret-here"

# Clerk frontend API URL (usually auto-configured)
NEXT_PUBLIC_CLERK_FRONTEND_API_URL="https://your-clerk-frontend-api.clerk.accounts.dev"

# -----------------------------------------------------------------------------
# STRIPE PAYMENT PROCESSING (Optional for Local Development)
# -----------------------------------------------------------------------------

# Stripe publishable key (safe to expose to frontend)
# Development: pk_test_xxx
# Production: pk_live_xxx
STRIPE_PUBLISHABLE_KEY="pk_test_your-stripe-publishable-key-here"

# Stripe secret key (server-side only, never expose)
# Development: sk_test_xxx
# Production: sk_live_xxx
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key-here"

# Stripe webhook secret for handling payment events
# Get this from your Stripe webhook configuration
STRIPE_WEBHOOK_SECRET="whsec_your-stripe-webhook-secret-here"

# -----------------------------------------------------------------------------
# FASTAPI PDF PROCESSING SERVICE (Optional for Local Development)
# -----------------------------------------------------------------------------

# FastAPI service URL
# Local development: http://localhost:8000
# Production: https://your-fastapi-service.onrender.com
FAST_API_URL="http://localhost:8000"

# Alternative format (some parts of code use this)
NEXT_PUBLIC_FAST_API_URL="http://localhost:8000"

# FastAPI secret key for API authentication
# Generate a strong random key for production
FAST_API_SECRET_KEY="your-strong-fastapi-secret-key-here"

# Landing AI API key for document processing
# Sign up at Landing AI to get this key
LANDING_AI_API_KEY="your-landing-ai-api-key-here"

# -----------------------------------------------------------------------------
# EMAIL SERVICE (RESEND) - Optional
# -----------------------------------------------------------------------------

# Resend API key for sending transactional emails
# Sign up at resend.com to get this key
RESEND_API_KEY="re_your-resend-api-key-here"

# From email address for transactional emails
# Must be verified in your Resend account
RESEND_FROM_EMAIL="hello@your-domain.com"

# Support email address
SUPPORT_EMAIL="support@your-domain.com"

# -----------------------------------------------------------------------------
# ERROR MONITORING (SENTRY) - Optional
# -----------------------------------------------------------------------------

# Sentry DSN for error tracking and monitoring
# Sign up at sentry.io to get this DSN
NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Sentry authentication token for releases and sourcemaps
SENTRY_AUTH_TOKEN="your-sentry-auth-token-here"

# Sentry organization slug
SENTRY_ORG="your-sentry-org-name"

# Sentry project slug
SENTRY_PROJECT="precision-pdf"

# -----------------------------------------------------------------------------
# ANALYTICS (POSTHOG) - Optional
# -----------------------------------------------------------------------------

# PostHog project API key for analytics tracking
# Sign up at posthog.com to get this key
NEXT_PUBLIC_POSTHOG_KEY="phc_your-posthog-project-key-here"

# PostHog API host (usually https://app.posthog.com)
NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# -----------------------------------------------------------------------------
# CUSTOMER SUPPORT (CRISP) - Optional
# -----------------------------------------------------------------------------

# Crisp website ID for embedded chat widget
# Sign up at crisp.chat to get this ID
NEXT_PUBLIC_CRISP_WEBSITE_ID="your-crisp-website-id-here"

# -----------------------------------------------------------------------------
# RATE LIMITING (UPSTASH) - Optional
# -----------------------------------------------------------------------------

# Upstash Redis URL for rate limiting
# Sign up at upstash.com to get these credentials
UPSTASH_REDIS_URL="redis://your-redis-url.upstash.io"
UPSTASH_REDIS_TOKEN="your-upstash-redis-token-here"

# -----------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# -----------------------------------------------------------------------------

# Environment mode
NODE_ENV="development"

# Enable debug logging (true/false)
DEBUG="false"

# Disable telemetry data collection (true/false)
NEXT_TELEMETRY_DISABLED="false"

# -----------------------------------------------------------------------------
# ADDITIONAL SECURITY SETTINGS (Production Only)
# -----------------------------------------------------------------------------

# JWT signing secret (generate a strong random string)
# Generate with: openssl rand -base64 64
JWT_SECRET="your-jwt-signing-secret-here"

# Session secret for additional security
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-session-secret-here"

# Encryption key for sensitive data
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="your-encryption-key-here"

# -----------------------------------------------------------------------------
# WEBHOOK URLS (Configured in external services)
# -----------------------------------------------------------------------------

# These are the webhook URLs you need to configure in external services:
#
# Clerk Webhooks:
# - User Sync: {NEXT_PUBLIC_APP_URL}/api/sync-user
#
# Stripe Webhooks:
# - Payment Events: {NEXT_PUBLIC_APP_URL}/.convex/http/stripe/webhook
#
# Make sure to configure these URLs in the respective service dashboards
# and use the corresponding webhook secrets above.

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env.local for local development
# 2. Fill in at minimum the NEXT_PUBLIC_CONVEX_URL (get from `npx convex dev`)
# 3. For FastAPI integration, run the FastAPI service locally or deploy it
# 4. For authentication, see docs/security/re-enabling-auth.md
# 5. For production, see docs/developers/deployment-guide.md
#
# SECURITY WARNING:
# - Never commit real API keys to version control
# - Use different keys for development and production
# - Rotate keys regularly in production
# - Keep production .env files secure and backed up
#
# =============================================================================